/* eslint-disable @typescript-eslint/no-explicit-any */
import Link from "next/link";
import { AppHeader } from "@/components/app-header";
import { createClient } from "@/lib/supabase/server";
import { updateInstagramStatus } from "./actions";

const tabs = [["performance","Performance"],["saved","Saved Items"],["following","Following"],["followers","Followers"]] as const;
const n = (value: unknown) => Number(value ?? 0).toLocaleString();

export default async function InstagramInsights({ searchParams }: { searchParams: Promise<{ tab?:string; sort?:string; direction?:string }> }) {
  const params = await searchParams; const tab = tabs.some(([key]) => key === params.tab) ? params.tab! : "performance";
  const supabase = await createClient(); const db = supabase as any;
  const [{ data:connections },{ data:posts },{ data:profiles },{ data:saved }] = await Promise.all([
    db.from("instagram_connections").select("*").limit(1), db.from("instagram_media").select("*").order("published_at",{ ascending:false }),
    db.from("instagram_profiles").select("*").order("fit_score",{ ascending:false }), db.from("instagram_saved_items").select("*").order("saved_at",{ ascending:false }),
  ]);
  const connection = connections?.[0]; const allPosts = posts ?? []; const allProfiles = profiles ?? []; const savedItems = saved ?? [];
  const reach = allPosts.reduce((sum:number,row:any)=>sum+Number(row.reach||0),0); const interactions = allPosts.reduce((sum:number,row:any)=>sum+Number(row.total_interactions||0),0);
  const rows = tab === "followers" ? allProfiles.filter((r:any)=>r.relationship_type === "followers") : allProfiles.filter((r:any)=>r.relationship_type === "following");
  return <main className="app-shell"><AppHeader /><section className="dashboard-head insights-head"><div><p className="eyebrow">Audience & Performance</p><h1>Instagram Insights</h1><p>{connection ? `@${connection.instagram_username ?? "Instagram"} · ${n(connection.followers_count)} followers · Last refreshed ${connection.last_synced_at ? new Date(connection.last_synced_at).toLocaleString() : "not yet"}` : "Instagram performance, saved content, and collaboration research."}</p></div><div className="button-row"><button>Connect Instagram</button><button className="primary">Refresh Insights</button></div></section>
  <nav className="insight-tabs">{tabs.map(([key,label])=><Link key={key} href={`/instagram-insights?tab=${key}`} data-active={tab===key}>{label}<span>{key==="performance"?allPosts.length:key==="saved"?savedItems.length:allProfiles.filter((r:any)=>r.relationship_type===key).length}</span></Link>)}</nav>
  {tab === "performance" ? <><section className="metric-grid"><article><span>Followers</span><strong>{n(connection?.followers_count)}</strong></article><article><span>Posts</span><strong>{n(allPosts.length)}</strong></article><article><span>Total Reach</span><strong>{n(reach)}</strong></article><article><span>Interactions</span><strong>{n(interactions)}</strong></article></section><section className="panel insights-table"><div className="insight-row insight-row-head"><span>Post</span><span>Views</span><span>Reach</span><span>Likes</span><span>Comments</span><span>Saved</span><span>Shares</span><span>Engagement</span></div>{allPosts.map((post:any)=><a className="insight-row" key={post.id} href={post.permalink ?? "#"} target="_blank"><span><strong>{String(post.caption||"Untitled post").slice(0,90)}</strong><small>{post.published_at?new Date(post.published_at).toLocaleDateString():"—"} · {post.media_product_type||post.media_type||"Post"}</small></span><span>{n(post.views)}</span><span>{n(post.reach)}</span><span>{n(post.like_count)}</span><span>{n(post.comments_count)}</span><span>{n(post.saved)}</span><span>{n(post.shares)}</span><span>{Number(post.engagement_rate||0).toFixed(2)}%</span></a>)}</section></> : tab === "saved" ? <section className="panel"><div className="panel-title"><div><h2>Saved Instagram Items</h2><p className="muted">Review inspiration saved from Instagram.</p></div><button>Import HTML Export</button></div><div className="profile-grid">{savedItems.map((item:any)=><article className="profile-card" key={item.id}><a href={item.instagram_url} target="_blank"><h3>{item.title||item.shortcode||"Saved item"}</h3><p>{item.content_overview||item.media_type||"No overview available."}</p></a><form action={updateInstagramStatus}><input type="hidden" name="table" value="instagram_saved_items"/><input type="hidden" name="id" value={item.id}/><select name="value" defaultValue={item.review_status}><option value="not_reviewed">Not Reviewed</option><option value="keep">Keep</option><option value="delete">Delete</option></select><button>Update</button></form></article>)}</div></section> : <section className="panel"><div className="panel-title"><div><h2>{tab === "followers" ? "Followers" : "Following & Collaboration Prospects"}</h2><p className="muted">Follower totals, fit analysis, and outreach status from V1.</p></div><button>Import Instagram HTML</button></div><div className="profile-grid">{rows.map((profile:any)=><article className="profile-card" key={profile.id}><div><a href={profile.profile_url} target="_blank"><h3>@{profile.username}</h3></a><strong>{profile.followers_count == null ? "Unavailable" : `${n(profile.followers_count)} followers`}</strong><p>{profile.biography||profile.content_analysis||"No profile summary available."}</p><small>{profile.fit_label||"Not analyzed"}{profile.fit_score!=null?` · Score ${profile.fit_score}`:""}</small></div><form action={updateInstagramStatus}><input type="hidden" name="table" value="instagram_profiles"/><input type="hidden" name="id" value={profile.id}/><select name="value" defaultValue={profile.collaboration_status}><option value="explore">Explore</option><option value="reached_out">Reached Out</option><option value="in_discussions">In Discussions</option><option value="in_place">In Place</option><option value="disqualified">Disqualified</option></select><button>Update</button></form></article>)}</div></section>}
  </main>;
}
